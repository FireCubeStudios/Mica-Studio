<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MicaStudio.Controls.Explorer.FolderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicaStudio.Controls.Explorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:winui="using:FluentIcons.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="4">
            <winui:FluentIcon Icon="Folder"/>
            <TextBlock Text="{x:Bind Folder.DisplayName, Mode=OneWay}"/>
        </StackPanel>
        <local:ExplorerListView Grid.Row="1" Folder="{x:Bind Folder, Mode=OneWay}"/>
    </Grid>
</UserControl>
